% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{glmcs}
\alias{glmcs}
\title{LASER: Likelihood-based Additive Single-Effect Regression algorithm for generalized linear regression problems}
\usage{
glmcs(
  X,
  y,
  family = gaussian("identity"),
  L = 10L,
  coverage = 0.95,
  standardize = TRUE,
  ties = "efron",
  method = "greedy",
  max_iter = 100L,
  step_size = 1,
  min_abs_corr = 0.5,
  tol = 1e-06,
  seed = NULL
)
}
\arguments{
\item{X}{A numeric \eqn{n \times p} predictor matrix.}

\item{y}{A response: numeric vector (for GLM) or \eqn{n\times2} matrix (time, status) for Cox.}

\item{family}{A GLM family object (e.g.\ \code{gaussian("identity")}, \code{binomial()})
or the string \code{"cox"}.}

\item{L}{Integer number of single-effect components (default \code{10L}
or \code{min(10, p)} if \code{p < 10}).}

\item{coverage}{Desired posterior mass for credible sets (default \code{0.95}).}

\item{standardize}{Logical; should predictors be standardized? (default \code{TRUE}).}

\item{ties}{Character; tie-handling method for Cox: \code{"efron"} or \code{"breslow"}
(default \code{"efron"}).}

\item{method}{Character; coordinate-ascent update order:
\code{"greedy"} or \code{"shuffle"} (default \code{"greedy"}).}

\item{max_iter}{Integer; maximum number of coordinate-ascent iterations
(default \code{100L}).}

\item{step_size}{Numeric; multiplicative step-size for updates (default \code{1.0}).}

\item{min_abs_corr}{Minimum absolute correlation threshold for purity filtering
(default \code{0.5}).}

\item{tol}{Numeric; convergence tolerance on log-likelihood change
(default \code{1e-6}).}

\item{seed}{Optional integer seed for reproducibility (default \code{NULL}).}
}
\value{
A named list with components:
\describe{
\item{\code{elapsed}}{Total fitting time (seconds).}
\item{\code{fit}}{Raw output from \code{\link{get_laser_fit}}.}
\item{\code{pmp}}{Posterior marginal probabilities of inclusion.}
\item{\code{theta}}{Estimated effect vector (sum over \eqn{L} components).}
\item{\code{cs}}{Credible sets (list of integer vectors) from
\code{\link{get_cs}}.}
\item{\code{coverage}}{Requested coverage level.}
\item{\code{keep}}{Logical vector of length \eqn{L}, indicating significant effects
via \code{\link{get_included}}.}
\item{\code{X}, \code{y}, \code{family}, \code{ties}, \code{method},
\code{step_size}, \code{seed}}{Echoed inputs.}
}
}
\description{
Fit the LASER model via blockwise coordinate ascent, perform variable selection under multicollinearity,
and build credible sets at a significant level. LASER supports general regression problems such as GLM built-in families and cox regression.
}
\examples{
\dontrun{
set.seed(42)
n <- 100; p <- 20
X <- matrix(rnorm(n * p), n, p)
beta <- c(3, -2, rep(0, p - 2))
y <- X \%*\% beta + rnorm(n)

res <- glmcs(
  X            = X,
  y            = y,
  family       = gaussian("identity"),
  L            = 5L,
  coverage     = 0.9,
  standardize  = TRUE,
  method       = "greedy",
  seed         = 123
)
print(res$theta)
print(res$cs)
}

}
\seealso{
\itemize{
\item \code{\link{get_laser_fit}} for the low‐level C++ fit routine
\item \code{\link{get_included}} to select significant effects
\item \code{\link{get_cs}} to build credible sets
}
}
