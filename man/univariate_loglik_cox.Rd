% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R, R/utils.R
\name{univariate_loglik_cox}
\alias{univariate_loglik_cox}
\title{Univariate Cox Partial Log-Likelihood}
\usage{
univariate_loglik_cox(x, y, offset = numeric(0), theta = 0, ties = "efron")

univariate_loglik_cox(x, y, offset = numeric(0), theta = 0, ties = "efron")
}
\arguments{
\item{x}{Numeric vector of covariate values}

\item{y}{Matrix with 2 columns (time, status)}

\item{offset}{Numeric vector or scalar offset}

\item{theta}{Coefficient value (default: 0)}

\item{ties}{Method for handling ties ("breslow" or "efron")}
}
\value{
A single numeric value: the partial log‐likelihood \eqn{\ell(\theta)}.

Log-likelihood value
}
\description{
Computes the partial log‐likelihood for a univariate Cox proportional hazards model,
allowing either the Breslow or Efron approximation to handle tied event times.
}
\examples{
\dontrun{
n <- 100
x <- rnorm(n)
time <- rexp(n, rate = exp(0.3 * x))
status <- rbinom(n, 1, 0.7)
y <- cbind(time, status)

# compute log‐likelihood at theta = 0.5
univariate_loglik_cox(x, y, offset = 0, theta = 0.5, ties = "efron")
}
}
\seealso{
\code{\link{univariate_irls_cox}}
}
