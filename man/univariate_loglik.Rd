% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{univariate_loglik}
\alias{univariate_loglik}
\title{Compute Univariate Log-Likelihood for GLM or Cox Model}
\usage{
univariate_loglik(x, y, family, theta = 0, offset = 0, ties = "efron")
}
\arguments{
\item{x}{Numeric vector of covariate values (length \(n\)).}

\item{y}{Response:
\itemize{
\item For GLMs: numeric vector of length \(n\).
\item For Cox: numeric \eqn{n\times2} matrix with columns (time, status).
}}

\item{family}{A GLM family object (e.g. \code{gaussian()}, \code{binomial()},
\code{poisson()}, etc.) or a Cox family list with \code{family = "cox"}.}

\item{theta}{Numeric scalar. The coefficient for the univariate model
(default: 0).}

\item{offset}{Numeric scalar or vector of length \(n\). Known offset for the
linear predictor (default: 0).}

\item{ties}{Character. Ties method for Cox partial likelihood:
\code{"efron"} (default) or \code{"breslow"}.}
}
\value{
A single numeric value: the log-likelihood of the univariate fit.
}
\description{
Calculates the log-likelihood of a single covariate effect under either a
generalized linear model (GLM) family or a Cox proportional hazards model.
}
\examples{
## Gaussian GLM
set.seed(1)
x <- rnorm(100)
y <- 2 * x + rnorm(100)
univariate_loglik(x, y, family = gaussian(), theta = 2)

## Binomial GLM (logit link)
set.seed(2)
x <- rnorm(200)
eta <- -1 + 1.5 * x
p  <- 1 / (1 + exp(-eta))
y  <- rbinom(200, size = 1, prob = p)
univariate_loglik(x, y, family = binomial(link = "logit"), theta = 1.5)

}
