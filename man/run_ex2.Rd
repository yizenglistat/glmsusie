% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_example2.R
\name{run_ex2}
\alias{run_ex2}
\title{Run Example 2 Benchmark Simulation}
\usage{
run_ex2(
  n_sims = 50L,
  n = 600L,
  p = 5L,
  L = 10L,
  family = gaussian(),
  rho = 0.9,
  intercept = -1,
  dispersion = 9,
  censoring_rate = 0.3,
  coverage = 0.95,
  methods = c("glmcs", "susie"),
  standardize = TRUE,
  ties = "efron",
  algorithm = "greedy",
  max_iter = 100L,
  step_size = 1,
  tol = 1,
  min_abs_corr = 0,
  parallel = TRUE,
  cores = parallel::detectCores() - 1L,
  save_path = NULL,
  save_freq = 50L,
  seed = 42L,
  progress = "bar",
  verbose = TRUE
)
}
\arguments{
\item{n_sims}{Integer. Number of simulation replicates to run. Default: 50.}

\item{n}{Integer. Number of observations in each simulation. Default: 600.}

\item{p}{Integer. Number of predictors. Default: 5.}

\item{L}{Integer. Number of components to include in the model. Default: 10.}

\item{family}{An R family object specifying the response distribution. Default: gaussian().}

\item{rho}{Numeric in (0,1). Correlation between predictors. Default: 0.9.}

\item{intercept}{Numeric. True intercept value. Default: -1.}

\item{dispersion}{Numeric. Dispersion parameter for the model. Default: 9.}

\item{censoring_rate}{Numeric in [0,1). Censoring rate for survival models. Default: 0.3.}

\item{coverage}{Numeric in (0,1). Target coverage level for confidence sets. Default: 0.95.}

\item{methods}{Character vector. Methods to evaluate: "glmcs", "susie", or both. Default: c("glmcs", "susie").}

\item{standardize}{Logical. Whether to standardize predictors. Default: TRUE.}

\item{ties}{Character. Method for handling ties in Cox models: "breslow" or "efron". Default: "efron".}

\item{algorithm}{Character. Algorithm to use: "greedy" or "forward". Default: "greedy".}

\item{max_iter}{Integer. Maximum number of iterations for optimization. Default: 100.}

\item{step_size}{Numeric. Step size for the gradient-based optimization. Default: 1.0.}

\item{tol}{Numeric. Convergence tolerance. Default: 1.0.}

\item{min_abs_corr}{Numeric in [0,1). Minimum absolute correlation threshold. Default: 0.0.}

\item{parallel}{Logical. Whether to run simulations in parallel. Default: TRUE.}

\item{cores}{Integer. Number of cores to use if parallel=TRUE. Default: detectCores() - 1.}

\item{save_path}{Character or NULL. Path to save intermediate results. Default: NULL.}

\item{save_freq}{Integer. Frequency of saving intermediate results. Default: 50.}

\item{seed}{Integer or NULL. Random seed for reproducibility. Default: 42.}

\item{progress}{Character or logical. Progress tracking method: "bar", "detailed", "text", FALSE. Default: "bar".}

\item{verbose}{Logical. Whether to display additional information. Default: TRUE.}
}
\value{
A list with benchmark results containing:
\describe{
\item{\code{coef_summary}}{List of data frames with coefficient recovery statistics.}
\item{\code{cs_summary}}{List of data frames with confidence set statistics.}
\item{\code{cover_summary}}{List of coverage rates for each method.}
\item{\code{timing_summary}}{Data frame with timing information for each method.}
\item{\code{elapsed_time}}{Total benchmark execution time.}
\item{\code{methods_used}}{Character vector of methods actually used.}
\item{\code{settings}}{List containing all benchmark parameters.}
}
}
\description{
Runs a comprehensive benchmark simulation for Example 2 settings, comparing
the performance of multiple variable selection methods. This function is a
convenience wrapper around the \code{\link{benchmark}} function with pre-configured
settings for the "ex2" simulation scenario.
}
\details{
This function configures and executes a simulation benchmark with the following characteristics:
\enumerate{
\item Generates synthetic data with controlled correlation structure (rho),
intercept, dispersion, and optional censoring for survival data
\item Compares method performance across multiple simulation replicates:
\itemize{
\item \strong{glmcs}: Uses the LASER algorithm for variable selection and confidence sets
\item \strong{susie}: Uses the Sum of Single Effects model (Gaussian family only)
}
\item Evaluates methods on:
\itemize{
\item Coefficient estimation accuracy
\item Confidence set coverage
\item Runtime performance
}
}

The "ex2" setting represents a specific simulation scenario with predefined
sparsity and coefficient structure designed to test confidence set construction
in challenging, correlated settings.
}
\examples{
\dontrun{
# Run with default settings (Gaussian response)
results <- run_ex2()

# Run with binomial response
bin_results <- run_ex2(
  n = 500,
  p = 10,
  family = binomial(),
  methods = "glmcs",  # SuSiE not compatible with non-Gaussian
  n_sims = 20
)

# Run with survival data
surv_results <- run_ex2(
  family = survival::coxph,
  censoring_rate = 0.4,
  methods = "glmcs",
  n_sims = 30
)

# Extract results
print(results$cover_summary)
print(results$timing_summary)
}

}
\seealso{
\code{\link{benchmark}}, \code{\link{simulate}}
}
